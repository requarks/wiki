common:
  header.followSite: "Follow Site"
  header.unfollowSite: "Unfollow Site"
  actions.saveNotify: "Save & Notify"
  actions.createNotify: "Create & Notify"
  actions.deleteNotify: "Delete & Notify"
admin:
  mail.allowedDomains: "Add Allow Only Sending mails to domains"
  mail.allowedDomainsHint: "Add a list of domains that are allowed to send emails to users. If the list is empty, all domains are allowed. Separate domains with a comma."
