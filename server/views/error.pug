extends master.pug

block body
  #root.is-fullscreen
    .app-error
      a(href=baseUrl + '/')
        img(src=baseUrl + '/_assets/svg/logo-wikijs.svg')
      strong Oops, something went wrong...
      span= message
            
      if error.stack
        pre: code #{error.stack}
