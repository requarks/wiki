extends master.pug

block body
  #root.is-fullscreen
    .app-error
      a(href='/')
        img(src='/_assets/svg/logo-wikijs.svg')
      strong Что-то пошло не так... Попробуйте перезагрузить страницу
      span= message
            
      if error.stack
        pre: code #{error.stack}
