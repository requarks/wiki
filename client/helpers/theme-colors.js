/**
 * Theme color hex values for JS/inline styles.
 * Must stay in sync with client/scss/base/material.scss (mc() palette).
 */

export const THEME_PRIMARY = '#1976D2'

/** mc('theme', 'brand') — Sunninoor brand navy. */
export const THEME_BRAND = '#192B85'

/** mc('theme', 'brand') — nav bar background and search field accents. */
export const NAV_BAR_COLOR = THEME_BRAND
export const THEME_INFO = '#2196F3'
export const THEME_SUCCESS = '#4CAF50'

/** mc('grey', '400') */
export const SCROLLBAR_GREY_DEFAULT = '#bdbdbd'

/** mc('grey', '500') */
export const SCROLLBAR_GREY_HOVER = '#9e9e9e'

/** mc('blue', '400') */
export const SCROLLBAR_BLUE_DEFAULT = '#42A5F5'

/** mc('blue', '300') */
export const SCROLLBAR_BLUE_HOVER = '#64b5f6'

/** Side drawer header text on mc('blue', '100') background. */
export const NAV_DRAWER_HEADER_FG = 'rgba(25, 43, 133, 0.82)'

/** Muted header text (email, secondary). */
export const NAV_DRAWER_HEADER_FG_MUTED = 'rgba(25, 43, 133, 0.58)'
