query ($query: String!) {
  pages {
    search(query:$query) {
      results {
        id
        title
        description
        path
        locale
        matches
        numMatches
      }
      suggestions
      totalHits
    }
  }
}
