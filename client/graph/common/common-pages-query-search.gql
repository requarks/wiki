query ($query: String!, $page: Int, $limit: Int) {
  pages {
    search(query:$query, page:$page, limit:$limit) {
      results {
        id
        title
        description
        snippet
        anchor
        path
        locale
      }
      suggestions
      totalHits
    }
  }
}
