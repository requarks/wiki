query ($query: String!, $siteId: String!) {
  pages {
    search(query:$query, siteId:$siteId) {
      results {
        id
        title
        description
        path
        locale
        siteId
      }
      suggestions
      totalHits
    }
  }
}
