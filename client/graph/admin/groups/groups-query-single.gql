query ($id: Int!) {
  groups {
    single(id: $id) {
      id
      name
      isSystem
      permissions
      pageRules {
        id
        path
        role
        exact
        allow
      }
      users {
        id
        name
        email
      }
      createdAt
      updatedAt
    }
  }
}
